Ogni agente modifica SOLO i file assegnati.

Non cambiare nomi file/cartelle senza scriverlo nelle note.

Tutto mobile-first.

Nessun framework: solo HTML/CSS/JS.

Le CTA devono “funzionare” anche in locale: se i webcomponent <ppn-cta> (o simili) non sono disponibili, usare fallback automatico (button/link) cliccabile

